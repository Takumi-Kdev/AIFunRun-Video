# AIFunRun-Video 依存（最小）
requests>=2.31
mcp>=2.0
pytest>=8

# 動画レンダリングには外部の ffmpeg が必要（OS にインストール）
# 3D 生成（任意・GPUメインPC）: torch / tsr / trellis / hy3dgen 等
pillow>=8.0

# 動画工場の字幕・日本語ナレーションを単体リポジトリでも利用可能にする。
faster-whisper>=1.2.1
pyttsx3>=2.99
piper-tts>=1.6.0
edge-tts>=7.2.8
